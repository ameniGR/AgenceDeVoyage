{% extends 'base.html.twig' %}

{% block title %}Etape index{% endblock %}

{% block main %}
    <h1>Etape index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NumeroEtape</th>
                <th>VilleEtape</th>
                <th>NombreJours</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etape in etapes %}
            <tr>
                <td>{{ etape.id }}</td>
                <td>{{ etape.numeroEtape }}</td>
                <td>{{ etape.villeEtape }}</td>
                <td>{{ etape.nombreJours }}</td>
                <td>
                    <a href="{{ path('admin_etape_show', {'id': etape.id}) }}">show</a>
                    <a href="{{ path('admin_etape_edit', {'id': etape.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_etape_new') }}">Create new</a>
    <br />
    <br />
    <h3>Règles de gestion à Ajouter:</h3>
    <ul>
      <li>la durée des circuits est égale à la somme des durées de ses étapes</li>
      <li>l’ordre des étapes d’un circuit est choisi par les agents (ou leurs sous-traitants) et peuvent être modifiés, indépendemment de l’ordre dans lequel les entités correspondantes ont été ajoutés dans la base de données de l’application.</li>
      <li>les villes de départ et d’arrivée d’un circuit correspondent à celles des premières ou dernières étapes de ce circuit</li>
      <li>etc</li>
	</ul>
{% endblock %}
